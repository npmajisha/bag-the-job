{{#each book}}
    <div class="col-md-2">
        <div class="row">
            <div class="col-md-12">
                <img src="{{thumbnail}}">
            </div>
        </div>
    </div>
{{/each}}

